import { is } from '../src/type-check';


describe('transaction', () => {

  it('', () => {
    // const arg = 2 as unknown;
    // if (is.number(arg)) {
    //   arg.toFixed()
    // }
    // expectType<string>(concat());
  })

})